-- MIN, MAX

-- Obtiene el valor menor del campo salario de la tabla "employees"
Select MIN(Salary) FROM employees;

-- Obtiene el valor mayor del campo salario de la tabla "employees"
Select MAX(Salary) FROM employees;